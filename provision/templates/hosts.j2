# {{ ansible_managed }}
    127.0.0.1   localhost
    ::1         localhost ip6-localhost ip6-loopback
    ff02::1     ip6-allnodes
    ff02::2     ip6-allrouters

    # Kubernetes Cluster Nodes
    {% for host_entry in all_cluster_hosts %}
    {{ host_entry.ip }}    {{ host_entry.name }}
    {% endfor %}
